const router = require("express").Router();

// router.get("/lists", (req, res) => {
  
//   res.render("search-movie");
// });

// router.post("/search", (req, res) => {
//   const obj = {
//     name: "olamundo",
//   };
  
//   res.render("search-movie", { movies: { name: "olamundo" } });
// });

module.exports = router;
